using IntelligencePipeline.Models.Reports;
using System;
using System.Collections.Generic;
using System.Text;

namespace IntelligencePipeline.Validation
{
    //class RadarValidator : BaseValidator
    //{
    //    protected override ValidationResult ValidateSpecificFields(Report report)
    //    {
    //        //megic number !!
    //        if (report.GetSourceType() != "Radar")
    //            return ValidationResult.Failure("type not allowed");
    //        if (report.Speed < 0 || report.Speed > 2000)
    //            return ValidationResult.Failure("only speed between 0 - 2000 allowed");
    //        if (report.Direction < 0 || report.Direction > 360)
    //            return ValidationResult.Failure("only direction between 0 - 360 allowed");
    //        if (report.Distance < 100 || report.Distance > 100000)
    //            return ValidationResult.Failure("only distance between 100 - 100000 allowed");
    //    }

        
    //}
}
